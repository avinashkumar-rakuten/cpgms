#include<stdio.h>
float main()
{
float b,h,area;
printf("Input the value of b: ");
scanf("%f", &b);
printf("Input the value of h: ");
scanf("%f", &h);
area=0.5*(b*h);
printf("Area of Triangle is: %f \n",area);
}
