#include<stdio.h>
float main()
{
float r,sector,theta;
printf("Input the value of r: ");
scanf("%f", &r);
printf("Input the value of theta: ");
scanf("%f", &theta);
sector=0.5*r*r*theta;
printf("Area of sector is: %f \n",theta);
}
